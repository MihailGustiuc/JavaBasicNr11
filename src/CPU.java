public class CPU {
    CPU cpu = new CPU();
    private class Processor {
        Processor processor = new Processor();

        // implementation omitted for brevity
    }

    // RAM inner class
    private class RAM {
        RAM ram = new RAM();
        // implementation omitted for brevity
    }
}
